/**
 * Code-level constants. Everything editorial (navigation, page copy, event data) lives in Sanity;
 * see src/sanity/content.ts.
 */

/** Colour family from the DevFest brand kit: one accent per page, card or chip. */
export type Family = 'yellow' | 'blue' | 'green' | 'red';
